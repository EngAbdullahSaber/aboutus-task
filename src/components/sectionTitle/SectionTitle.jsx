import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <h2 className="py-9 font-bold text-center text-3xl text-[#343A40]">
      {title}
    </h2>
  );
};

export default SectionTitle;
